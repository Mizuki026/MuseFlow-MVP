# 仓库协作规范

## 规则强度与优先级

- “必须”和“不得”表示不可自行省略的硬性要求。
- “默认”表示在用户没有明确要求、项目现状也没有相反约束时应采用的做法。
- “仅当”表示规则只有在所列条件满足后才生效。
- 用户在当前任务中的明确指令优先于本文件中的默认规则。若用户选择的方案存在明显风险，必须先说明风险和取舍，但不得擅自改成其他目标。

## 核心协作原则

- 开始复杂任务前，必须明确目标、背景、范围、明确不做的内容、关键约束、不可妥协项和验收标准。小型且清晰的任务可以根据请求和仓库上下文直接判断，不得为形式完整而制造流程。
- 必须先读取与任务相关的代码、文档、测试、配置和现有实现模式，再作出修改。
- 信息不足时，应优先从仓库和可用工具中查证事实；只有需要用户作出实质决策时才询问。
- 不能把“已经修改”当作完成。每项交付都必须通过测试、检查或可复现的最小验证形成闭环。

## 任务判断与工作模式

- 选择方案前，必须判断任务属于长期建设、阶段性过渡还是短期止血。方案复杂度、兼容策略和验证范围必须与任务的时间尺度匹配。
- 采用临时方案时，必须明确标注限制、风险和建议的后续处理，不得把临时方案描述成长期完成状态。
- 根据任务所处阶段切换工作模式：
  1. 探索：目标尚不清楚时，先调查现状，提出 3 至 5 个按优先级排列的建议，并说明各自适用前提。
  2. 规划：目标已知但路径未定时，明确范围、取舍、实施路径和验收标准。
  3. 实施：方向清楚时，读取上下文后持续推进，不因非关键细节频繁中断。
  4. 验证：使用测试、构建、静态检查或验收步骤证明结果成立。

## 自主推进与询问边界

- 任务目标和方向明确时，默认直接推进，并选择与现有项目模式一致、风险最低且最容易验证的合理方案。
- 仅当缺失信息会导致下列情况之一时，才应向用户集中提问：
  - 不同答案会实质改变产品行为或验收结果；
  - 会明显扩大任务范围、成本或交付时间；
  - 会破坏外部契约、持久化数据或兼容性；
  - 涉及不可逆操作、安全或隐私风险；
  - 无法从代码、文档、测试和现有模式中可靠推断。
- 不得因命名、排版或局部实现等可自行判断的非关键问题频繁打断任务。
- 不得在没有授权的情况下把任务扩展为无关的重构、清理或历史问题修复。

## 工程质量与兼容性

- 默认在任务范围内优先选择中长期可维护、健壮且便于扩展的方案，不得为了短期通过而隐藏已知问题或制造难以回收的技术债。
- 方案复杂度必须与当前需求、已知演进方向和风险匹配，不得为没有明确需求的假设场景提前设计抽象层或扩展能力。
- 仅当用户明确授权不保留兼容性时，才可以直接切换到目标状态。此时必须：
  - 识别并同步修改仓库内所有调用方；
  - 检查公开 API、持久化数据、配置格式和外部消费者；
  - 在需要时提供迁移方案或明确说明破坏性影响；
  - 删除已经无用的兼容层、桥接逻辑和过渡代码；
  - 用测试和验证证明目标状态成立。

## 测试与验证

- 每次行为改动都必须编写或更新相关测试；缺陷修复应优先增加能够复现问题并防止回归的测试。
- 必须先运行直接相关的测试。跨模块、高风险、架构调整或发布相关改动还必须运行项目适用的完整测试、构建、lint 和类型检查。
- 当前改动引入或影响的测试与验证必须全部通过，才能提交和交付。不得提交明知由当前改动造成的失败。
- 若发现与当前任务无关的既有失败，不得隐瞒或误称全部通过。必须证明其在修改前已经存在，记录具体失败，并避免擅自扩大任务范围。
- 没有适用的自动化测试时，必须执行与改动类型匹配的替代验证，并在交付时说明：
  - 文档：检查内容、链接、格式和 diff；
  - 配置：执行解析或配置校验；
  - UI：执行构建并检查关键交互，必要时进行截图核验；
  - 重构：运行相关测试并确认对外行为未改变；
  - 无测试框架的小项目：执行最小可复现的运行或静态检查。
- 不得声称未实际运行的测试或验证已经通过。

## 文档沉淀

- 满足以下任一条件的复杂任务，必须优先更新已有方案或进度文档；没有合适文档时才创建新文档：
  - 跨多个会话或阶段；
  - 涉及重要架构决策或数据迁移；
  - 包含多个可独立交付的里程碑；
  - 预计需要其他人接手。
- 复杂任务文档至少应记录目标与背景、范围与非目标、关键约束、实施方案、重要决策、当前进度、下一步、验收标准和验证结果。
- 只在完成有意义的阶段后更新进度，不得为普通单文件修改或每次编辑创建无价值的过程文档。

## Git 与提交规范

- 开始修改前和提交前都必须检查工作区状态，识别用户已有的未提交改动。
- 默认不得暂存、修改、覆盖或提交与当前任务无关的用户改动。若当前任务与既有改动重叠且无法安全分离，必须先向用户说明。
- 每完成一个逻辑完整、可独立理解、已经验证且可独立回滚的改动单元，都必须创建对应的 Git commit。不得为了拆小提交而提交无法独立成立的半成品。
- 提交前必须检查暂存区和最终 diff，确认提交只包含当前逻辑单元的预期内容。
- commit message 必须采用 Conventional Commits 格式，类型使用 `feat`、`fix`、`docs`、`test`、`refactor`、`chore` 等英文标识，描述使用简洁中文，例如 `docs: 完善仓库协作规范`。
- 若用户明确要求本次任务不要提交，则遵循用户指令，但仍必须完成所有适用验证并报告未提交状态。

## 审查与建议类任务

- 当用户要求分析、审查、指出问题或给建议时，只进行诊断和报告，不得在未获授权时直接修改。
- 审查结果必须按严重程度和优先级排序，并包含具体证据或位置、实际影响和推荐方向。
- 必须明确区分已确认的事实、基于证据的推断和可选优化，不得把个人偏好包装成缺陷。
- 当任务仍然模糊时，应先提出 3 至 5 个最值得优先考虑的建议及其适用前提；当用户明确要求实现、修复、创建或改造后，再进入实施模式。
